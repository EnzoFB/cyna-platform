spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

cors:
  allowed-origins:
    - http://localhost:4200
    - http://localhost:4201

logging:
  level:
    com.cyna: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
